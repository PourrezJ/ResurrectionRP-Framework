using AltV.Net;
using AltV.Net.Async;
using AltV.Net.Elements.Entities;
using System.Threading.Tasks;

namespace ResurrectionRP_Server.Factions
{/* TODO
    public class FactionsCommands : ICommandHandler
    {
        [Command("addonu")]
        public async Task AddPlayerONU(IPlayer client)
        {
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.Onu.TryAddIntoFaction(client, 4);
        }

        [Command("addlspd")]
        public async Task AddPlayerLSPD(IPlayer client)
        { 
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.Lspd.TryAddIntoFaction(client, 4);
        }

        [Command("addrebelle")]
        public async Task AddPlayerRebelle(IPlayer client)
        {
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.Rebelle.TryAddIntoFaction(client, 3);
        }

        [Command("addlscustom")]
        public async Task AddPlayerLSCustom(IPlayer client)
        {
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.LSCustom.TryAddIntoFaction(client, 1);
        }

        [Command("addgouv")]
        public async Task AddPlayerGouv(IPlayer client)
        {
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.Gouvernement.TryAddIntoFaction(client, 7);
        }

        [Command("adddock")]
        public async Task AddPlayerDock(IPlayer client)
        {
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.Dock.TryAddIntoFaction(client, 5);
        }

        [Command("addsheriff")]
        public async Task AddPlayerSheriff(IPlayer client)
        {
            if (PlayerManager.GetPlayerByClient(client).StaffRank <= AdminRank.Player) return;
            await GameMode.Instance.FactionManager?.Nordiste.TryAddIntoFaction(client, 5);
        }
    }*/
}

